name: k3s-master
description: Master Node, K3S
runtime:
  name: python
  options:
    toolchain: pip
    # Point this to the root venv
    virtualenv: ../../venv
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python