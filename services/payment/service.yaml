apiVersion: v1
kind: Service
metadata:
  name: payment-service
spec:
  selector:
    app: payment-app # must be same as metadata.labels.app from deployment
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 4005

